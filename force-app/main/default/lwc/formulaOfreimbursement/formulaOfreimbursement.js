import { LightningElement } from 'lwc';
import Formula from '@salesforce/resourceUrl/Formula'; // Importation de l'image depuis Static Resources

export default class FormulaOfreimbursement extends LightningElement {
     imageUrl = Formula; 
}
