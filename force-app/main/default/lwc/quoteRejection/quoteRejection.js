import { LightningElement } from 'lwc';
import quoteRejection from '@salesforce/resourceUrl/quoteRejection';
export default class QuoteRejection extends LightningElement {
    imageUrl = quoteRejection
}